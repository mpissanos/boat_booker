<table class="table table-striped table-hover" id="trips-table">
    <thead>
      <tr>
        <th>Date</th>
        <th>Location</th>
        <th>Boat</th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
    <% @trips.each do |trip|%> 
      <tr>
        <td id="clientName"><%= @client.name %></td>
        <td id="clientEmail"><%= @client.email %></td>
        <td id="clientPhoneNumber"><%= @client.phone_number %></td>
        <td id="editBclientButton"><%= link_to "Edit", edit_client_path(client), class: "btn btn-primary" %>
        </td>
        <td id="deleteclientButton"><%= link_to "Delete", client_path(client), class: "btn btn-primary", data: {:confirm => "Are you sure?" }, method: :delete %></td>
      </tr>
      <% end %>
    </tbody>
  </table>